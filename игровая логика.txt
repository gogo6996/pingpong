/* const arenaWidth = 800;
    const arenaHeight = 500;
    const paddleWidth = 16;
    const paddleHeight = 90;
    const ballSize = 18;

    const leftPaddle = document.getElementById('left-paddle');
    const rightPaddle = document.getElementById('right-paddle');
    const ball = document.getElementById('ball');
    const p1ScoreElem = document.getElementById('p1-score');
    const p2ScoreElem = document.getElementById('p2-score');
    const messageScreen = document.getElementById('message-screen');
    const msgTitle = document.getElementById('msg-title');
    const msgSub = document.getElementById('msg-sub');

    let p1Score = 0;
    let p2Score = 0;
    let isPlaying = false;

    let p1Y = 205;
    let p2Y = 205;
    const paddleSpeed = 7;

    let ballX = 391;
    let ballY = 241;
    let ballVX = 0;
    let ballVY = 0;
    const baseBallSpeed = 4;

    const keys = {
      w: false,
      s: false,
      ArrowUp: false,
      ArrowDown: false
    };

    window.addEventListener('keydown', (e) => {
      if (e.key in keys) keys[e.key] = true;
      if (e.code === 'Space' && !isPlaying) {
        startGame();
      }
    });

    window.addEventListener('keyup', (e) => {
      if (e.key in keys) keys[e.key] = false;
    });

    function startGame() {
      isPlaying = true;
      messageScreen.style.display = 'none';
      resetBall(Math.random() > 0.5 ? 1 : -1);
    }

   function resetBall(direction) {
      ballX = (arenaWidth - ballSize) / 2;
      ballY = (arenaHeight - ballSize) / 2;
      const angle = (Math.random() * 40 - 20) * (Math.PI / 180);
      ballVX = direction * baseBallSpeed * Math.cos(angle);
      ballVY = baseBallSpeed * Math.sin(angle);
    }

    function update() {
      if (isPlaying) {
        if (keys.w) p1Y = Math.max(0, p1Y - paddleSpeed);
        if (keys.s) p1Y = Math.min(arenaHeight - paddleHeight, p1Y + paddleSpeed);
        
        if (keys.ArrowUp) p2Y = Math.max(0, p2Y - paddleSpeed);
        if (keys.ArrowDown) p2Y = Math.min(arenaHeight - paddleHeight, p2Y + paddleSpeed);

        leftPaddle.style.top = `${p1Y}px`;
        rightPaddle.style.top = `${p2Y}px`;

        ballX += ballVX;
        ballY += ballVY;

        if (ballY <= 0) {
          ballY = 0;
          ballVY = -ballVY;
        } else if (ballY >= arenaHeight - ballSize) {
          ballY = arenaHeight - ballSize;
          ballVY = -ballVY;
        }

        if (ballVX < 0 && ballX <= 20 + paddleWidth && ballX >= 20) {
          if (ballY + ballSize >= p1Y && ballY <= p1Y + paddleHeight) {
            ballX = 20 + paddleWidth;
            ballVX = -ballVX * 1.05;
            let impact = (ballY + ballSize / 2) - (p1Y + paddleHeight / 2);
            ballVY += impact * 0.1;
          }
        }

        if (ballVX > 0 && ballX + ballSize >= arenaWidth - 20 - paddleWidth && ballX + ballSize <= arenaWidth - 20) {
          if (ballY + ballSize >= p2Y && ballY <= p2Y + paddleHeight) {
            ballX = arenaWidth - 20 - paddleWidth - ballSize;
            ballVX = -ballVX * 1.05;
            let impact = (ballY + ballSize / 2) - (p2Y + paddleHeight / 2);
            ballVY += impact * 0.1;
          }
        }

        if (ballX < 0) {
          p2Score++;
          p2ScoreElem.innerText = p2Score;
          checkWinState(-1);
        } else if (ballX > arenaWidth) {
          p1Score++;
          p1ScoreElem.innerText = p1Score;
          checkWinState(1);
        }

        ball.style.left = `${ballX}px`;
        ball.style.top = `${ballY}px`;
      }

      requestAnimationFrame(update);
    }

  function checkWinState(lastLoserDirection) {
      if (p1Score >= 11 || p2Score >= 11) {
        isPlaying = false;
        msgTitle.innerText = p1Score >= 11 ? "ИГРОК 1 ПОБЕДИЛ!" : "ИГРОК 2 ПОБЕДИЛ!";
        msgSub.innerText = "Нажмите ПРОБЕЛ, чтобы повторить";
        p1Score = 0;
        p2Score = 0;
        p1ScoreElem.innerText = 0;
        p2ScoreElem.innerText = 0;
        messageScreen.style.display = 'block';
      } else {
        resetBall(lastLoserDirection);
      }
    }
    update();

css:
:root {
  
      --retro-yellow: #FFC567;
      --retro-pink: #FB7DA8;
      --retro-coral: #FD5A46;
      --retro-purple: #552CB7;
      --retro-green: #00995E;
      --retro-blue: #058CD7;
      
      --bg-color: #0d1117; 
    }

    body {
      margin: 0;
      padding: 0;
      background-color: var(--bg-color);
      color: #ffffff;
      font-family: 'Courier New', Courier, monospace;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      overflow: hidden;
      user-select: none;
    }

    #game-arena {
      position: relative;
      width: 800px;
      height: 500px;
      border: 6px solid var(--retro-purple);
      box-sizing: border-box;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.7);
      
      background-image: url('f3915ae6f2f0bccb33e19b66089c58ed.jpg'); 
      background-size: cover; 
      background-position: center; 
      background-repeat: no-repeat;
    }

    .center-line {
      position: absolute;
      left: 50%;
      top: 0;
      bottom: 0;
      width: 4px;
      transform: translateX(-50%);
      border-left: 4px dashed var(--retro-purple);
      opacity: 0.6;
    }

    .score-board {
      position: absolute;
      top: 20px;
      width: 100%;
      display: flex;
      justify-content: space-around;
      font-size: 5rem;
      font-weight: bold;
      pointer-events: none;
    }

    #p1-score {
      color: var(--retro-pink);
      text-shadow: 2px 2px 0px var(--retro-purple);
    }

    #p2-score {
      color: var(--retro-pink);
      text-shadow: 2px 2px 0px var(--retro-purple);
    }

    .score {
      width: 150px;
      text-align: center;
    }

    /* Ракетки (DOM-элементы) */
    .paddle {
      position: absolute;
      width: 16px;
      height: 90px;
      border-radius: 4px;
      top: 205px;
      will-change: top;
      box-shadow: 3px 3px 0px rgba(0, 0, 0, 0.3);
    }

    #left-paddle { 
      left: 20px; 
      background-color: var(--retro-coral);
    }
    
    #right-paddle { 
      right: 20px; 
      background-color: var(--retro-blue);
    }

    #ball {
      position: absolute;
      width: 18px;
      height: 18px;
      background-color: var(--retro-yellow);
      border-radius: 50%;
      left: 391px;
      top: 241px;
      will-change: left, top;
      box-shadow: 2px 2px 0px rgba(0, 0, 0, 0.3);
    }

    #message-screen {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      text-align: center;
      background: var(--retro-green);
      padding: 25px 40px;
      border: 4px solid var(--retro-yellow);
      box-shadow: 6px 6px 0px var(--retro-purple);
    }

    #message-screen h2 {
      margin: 0 0 10px 0;
      font-size: 1.8rem;
      color: #ffffff;
      letter-spacing: 1px;
      text-shadow: 2px 2px 0px var(--retro-purple);
    }

    #message-screen p {
      margin: 0;
      font-size: 0.9rem;
      font-weight: bold;
      color: var(--retro-yellow);
    }

    html:
    <!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Retro Pong</title>
  <link rel="stylesheet" href = "ping.css">
</head>
<body>

  <div id="game-arena">
    <div class="center-line"></div>
    
    <div class="score-board">
      <div id="p1-score" class="score">0</div>
      <div id="p2-score" class="score">0</div>
    </div>

    <div id="left-paddle" class="paddle"></div>
    <div id="right-paddle" class="paddle"></div>
    <div id="ball"></div>

    <div id="message-screen">
      <h2 id="msg-title">РЕТРО ПОНГ</h2>
      <p id="msg-sub">Нажмите пробел для старта</p>
    </div>
  </div>

  <script src = "pong.js"></script>
</body>
</html>